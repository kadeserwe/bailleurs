version: '2'
services:
  bailleurs-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/bailleurs/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=bailleurs
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
